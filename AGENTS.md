# 끝없는 검로 — 개발 인수인계 문서

이 리포지토리에서 작업하는 사람과 AI 모두 **코드를 고치기 전에 이 문서를 끝까지 읽으세요.**
여기 적힌 방향성과 규칙은 지금까지의 설계 결정을 담고 있습니다. 규칙을 바꿔야 한다면 코드와 함께 이 문서도 고치세요.

- 플레이: https://hical3111-pixel.github.io/original/
- 저장소: https://github.com/hical3111-pixel/original
- 자가 점검: 주소 뒤에 `?selftest` (예: `http://localhost:5173/?selftest`)

---

## 1. 우리가 추구하는 방향

1. **타격감과 이펙트가 최우선이다.** 모든 공격은 히트스톱(`stop`), 화면 흔들림(`addTrauma`), 줌 펀치(`zoom`), 섬광(`flash`), 파티클 중 여러 개를 함께 쓴다. 큰 한 방에는 화면 균열(`crack`)과 컷인(`cutin`)까지 쓴다. "맞았다"는 느낌이 없는 스킬은 미완성이다.
2. **방치형이지만 보는 재미가 있어야 한다.** 가만히 둬도 자동 스킬, 연계기, 보스 패턴, 컷신이 계속 볼거리를 만든다. 손을 대면(탭, 패링, 수동 스킬) 더 큰 보상을 준다.
3. **레퍼런스 화풍을 따른다.** 사용자가 보낸 이펙트 콘셉트 아트의 문법: **단색 레이어를 겹친 평면 색**, **밝게 빛나는 중심(흰색·가산 합성)**, **검은 연기와 먹물**, **회색 파편·바위**, 실루엣 캐릭터. 사진 같은 질감이나 그라데이션 남발은 피한다.
4. **연계가 전투의 중심이다.** 모든 일반 스킬은 정확히 하나의 연계기에 속한다. 자동 스킬도 연계를 우선한다(4장 참고).
5. **직관적으로 안내한다.** 새 요소는 화면 안에서 스스로 설명돼야 한다: 버튼 배지, 안내 칩, 컷인 부제, 패널 카드, "시연" 버튼. 설명 없이 숨은 규칙을 만들지 않는다.
6. **외부 의존성이 없다.** 이미지·사운드 파일, 라이브러리, 빌드 단계가 없다. 모든 그림은 캔버스 절차 생성, 모든 소리는 WebAudio 합성이다. 이 원칙을 깨려면 사용자와 먼저 상의한다.
7. **한국어 UI, 모바일 대응.** 문구는 한국어, 폭 390px 휴대폰에서도 조작 가능해야 한다. `prefers-reduced-motion`이면 흔들림·섬광을 줄인다(`FXS`, `RM`).

---

## 2. 파일 구조와 로드 순서

| 파일 | 역할 |
|---|---|
| `index.html` | 화면 뼈대, CSS 전체, 스크립트 로드. 스크립트는 캐시를 피하려고 `?v=시각`을 붙여 순서대로(`async=false`) 불러오며, `?selftest`면 `selftest.js`를 이어서 불러온다. `<script src>`를 직접 추가하지 말고 이 목록에 넣는다 |
| `core.js` | 유틸, 상태(`S`)·저장, 일일 도전, 유물·업적 데이터, 스탯(`stats`), 강화(`UP`), 사운드(`sfx`), 월드 전역 변수, 몬스터 생성, 연출 도우미, 전투(`heroStrike`, `deal`, `skillHit`, `kill`) |
| `skills.js` | 스킬 그리기 도우미, 모든 스킬·연계기·각성기 함수, `SK`/`COMBOS`/`AWK`/`BR`/`IC` 데이터, 시전(`cast`)·자동 시전(`autoCast`), 유물 상자, 분기 효과(`branchFX`) |
| `main.js` | `resize`, 보스 패턴·등장 컷신·2페이즈, 동료, `update`(게임 루프), `render`와 모든 `draw*`, 패널 UI(`build*`, `uiTick`), 오프라인 보상, 부팅(`start`) |
| `selftest.js` | 구조 자가 점검(6장) |
| `README.md` | 소개용. 개발 규칙은 이 문서에 둔다 |

- **모듈이 아닌 일반 `<script>` 3개가 전역 스코프를 공유한다.** 최상위 `const`/`let`/`function`은 다른 파일에서도 보인다. 그래서 **최상위 이름이 파일 간에 겹치면 안 된다.** 새 전역 이름을 만들기 전에 세 파일을 검색하라.
- 로드 순서는 `core.js → skills.js → main.js`. 로드 시점에 즉시 실행되는 코드는 앞 파일의 것만 참조할 수 있다. 함수 본문 안의 참조는 실행 시점에 풀리므로 순서와 무관하다.
- 코드는 한 줄에 여러 문장을 쓰는 **압축 스타일**이다. 주변 코드와 같은 밀도·이름 규칙을 유지하라. 대규모 재포맷 금지(차이 비교가 불가능해진다).

---

## 3. 핵심 구조

### 3-1. 게임 루프와 시간
- `frame` → `update(rdt)` → `render()`. `rdt`는 실제 경과 시간(최대 0.05초).
- `update`는 배너·컷인·UI 보간·히트스톱·카메라를 실제 시간으로 처리하고, `updateWorld`에는 `gameSpeed()`를 곱한 게임 시간을 최대 1/60초씩 나눠 전달한다. 월드·공격·이동·쿨타임·FX·보스 패턴/제한 시간은 함께 빨라지며, 보스 등장 컷신은 실제 시간이다.
- **히트스톱:** `stop>0`인 동안 `update`는 연출 타이머(배너·컷인·균열·잉크)만 진행하고 월드는 멈춘다. 큰 타격에 `stop=Math.max(stop,0.1)`처럼 쓴다.
- **슬로모션:** `slowT`. 보스 처치 등.
- **카메라:** `trauma`(흔들림, 제곱해서 적용), `zoom`(1로 복귀), `flash(세기,'r,g,b')`, `invertT`(색 반전), `desat`(흑백. 매 프레임 스스로 줄어들므로 계속 흑백이어야 하면 연출이 매 프레임 다시 설정), `dimT`/`tintA`·`tintC`(매 프레임 0으로 초기화되므로 연출이 매 프레임 설정).
- **좌표:** 모든 크기는 `U`(화면 크기 비례 배율)를 곱한다. 지면은 `groundY`, 기사는 `heroX`, 몬스터 기본 위치는 `monX`. 절대 픽셀 상수를 쓰지 않는다.

### 3-2. 연출 시스템 `addFX`
스킬·연계기·각성기의 모든 연출은 FX 객체 하나다.
```js
addFX({dur:1.5, up(dt,o){...}, back(o){...}, draw(o){...}, post(o){...}, end(o){...}})
```
- `o.t`는 경과 시간(자동 증가). `dur`에 도달하면 `end`를 호출하고 제거한다.
- `back`: 몬스터 뒤에 그림, `draw`: 몬스터·기사 앞에 그림, `post`: 흑백 처리(`desat`) 뒤에 그림.
- 한 번만 실행할 이벤트는 `at(o, 시간, fn)`.
- ⚠️ **FX 객체의 예약 이름: `t, dur, f, up, back, draw, post, end`.** 이 이름을 상태 플래그로 쓰면 연출 시스템이 망가진다(실제로 `o.back=1`이 `back()` 호출 오류를 낸 적이 있다). 플래그는 `o.hit`, `o.rt`처럼 다른 이름을 쓴다.
- 짧은 지연 실행은 `later(초, fn)`.

### 3-3. 파티클 `P.push({t:'종류', ...})`
종류: `spark, dot, ring, glow, star, streak, slash, dslash, shard, smoke, blob, spike, flame, ghost, bolt`. `main.js`의 `drawParticles`에 그리기 코드가 있다. 새 종류가 필요하면 거기에 추가한다. 도우미: `burst`, `ink`, `smoke`, `spike`, `rubble`, `genBolt`. 파티클은 1300개에서 잘린다.

### 3-4. 피해 처리
- 기사 평타 `heroStrike`, 스킬 `skillHit(배율, pm, x, y, 옵션)`, 내부 공통 `deal`.
- `pm`은 시연용 배율(실전 1, 시연 0.1). **모든 스킬 함수는 `pm`을 받아 `skillHit`에 그대로 넘긴다.**
- 옵션: `light`(작은 연타, 히트스톱 없음), `heavy`(큰 한 방: 히트스톱·흔들림·섬광), `name`(데미지 숫자 위 라벨), `col`(숫자 색), `fc`(섬광 색 'r,g,b'), `crack`(1 작은 균열, 2 큰 균열), `sid`(스킬 id. 분기 효과·업적 판정에 쓰임, 연계기는 `'combo'`).
- 속박(`m.vuln>0`)이면 받는 피해 ×1.3.

### 3-5. 시전 잠금 `castLock`
- 스킬 연출이 진행되는 동안 평타·다른 스킬·탭 공격을 막는다.
- 연출이 긴 스킬은 `up` 안에서 매 프레임 `castLock=Math.max(castLock,.05)`로 잠금을 유지한다. 끝나면 자연히 풀린다.
- 버프형 스킬(`buff:1`, 예: 악귀 변신)은 잠금을 쓰지 않는다.

### 3-6. 몬스터와 보스
- 상태 `m.state`: `enter → fight → (phase) → dead | split | flee`. 전투 판정은 항상 `fighting()`으로 한다. **스킬 연출 중 몬스터가 죽을 수 있으므로 `m ? mCenter(m) : 대체 좌표` 패턴을 지킨다.**
- 보스(`stage%5===0`): 등장 컷신(`BI`, 탭하면 건너뜀) → 전투 → 체력 50%에서 2페이즈(`startPhase2`, 분노·새 패턴 `rain`) → 처치 시 결정타 연출(`BF`, 마지막 피해 공격 이름 중앙 표시 + 슬로모션·흔들림·섬광) + 두 동강(`split`) + 유물 상자.
- 보스 패턴 `slam, beam, charge, rain, orbs`(그림자 추적탄: STAGE 20 이상, 3개/2페이즈 4개, 0.4초 간격). 적중 직전 0.5초 안에 탭하면 패링하며, 추적탄은 구체마다 별도 금색 경고·패링 판정이다. 전부 패링하면 '완벽 반사!'로 공격력 12배 반격. 결정 방패는 적중 시 켜져 있는 구체만 기존 막기/파쇄검으로 처리한다.
- 기사 HP는 없고 피격 피해는 기절이다. 추적탄은 기존 공격의 총 기절 1.3초를 구체 수로 나누며, 부분 패링/막기는 해당 몫만 없앤다. 회피 55%는 묶음당 한 번 판정해 방치 플레이와 유리 대포 도전의 피격 확률을 기존과 같게 유지한다.
- 제한 시간 `bossTime()`(30초 + 유물 보너스, 일일 도전 60초). 실패하면 반복 사냥, 재도전 시 **모든 스킬 쿨타임 초기화**.

### 3-7. 저장
- `localStorage` 키 `blade-road-v1`. 5초마다·탭 숨김 시 저장.
- **키를 바꾸지 말고, 필드를 지우지 말 것.** 새 필드는 `fresh()`에 기본값을 넣고, 객체형이면 `load()`에서 병합한다. 기존 플레이어 저장이 그대로 이어져야 한다.
- 일일 도전 중에는 `save()`가 원래 스테이지(`CH.saved`)로 바꿔 저장한다.

### 3-8. 계열·편성·집중·공명
- `COMBOS.school`로 모든 연계를 9계열(`SCHOOLS`, 각 2쌍)에 배정하며, 각 `awk`는 전용 각성기 id다. 집중 상태에서 선택한 전용 각성기는 기존 `skillHit` 경로로 피해 +50%를 받으며, 천검멸은 기본 각성기다. `RESONANCES`는 기존 9쌍을 유지하고 음양검결–무극도법(태허도검), 녹광–무극도법(생무극)을 더한 11쌍이다. 월식·심연·음양검결·녹광의 이웃은 3개, 나머지는 2개다.
- `S.loadout`은 시작 스킬 id를 순서대로 최대 4개 저장한다(기본 `['dash']`). `setLoadout`/`syncEquip`이 각 쌍의 두 스킬로 기존 `S.equip`을 채운다. 잠긴 쌍도 편성 가능하며, 예전 저장은 장착 순서로 쌍을 추정하고 앞 4쌍 유지·빈칸 추천 보충; 새 저장의 빈칸/빈 편성은 보존한다.
- `schoolState(loadout,best)`는 **두 스킬이 모두 해금된 쌍만** 세어 집중·공명 id를 돌려준다. `recommendLoadout(best,keep)`은 한 스킬 이상 열린 쌍에서 집중+공명 총수를 최대화하며, 동률이면 완성된 쌍 수→열린 스킬 수→데이터 순서로 고른다. 숙련은 `S.mastery`에 계열별 저장하며 10/25에서 집중 2/3단계가 열린다. 설명은 `FOCUS_FX`·`RES_FX`에 둔다.
- 음양검결 집중은 연계 발동당 흑백 일섬(공격력 ×4), 2단계 연계 창 +2초, 3단계 수묵 FX(`inkSchool`) 중 치명타 +20%p다. 묵월은 실전 연계 적중 **후** `m.inkMark` 5게임초를 부여·갱신해 이후 받는 피해 +15%; 현묵은 발동당 20%로 마무리 쿨타임을 초기화한다. 시연은 공명 발동에서 제외한다.
- 무극도법 집중은 연계 시 다음 보스 공격 1회 흡수 준비(발동부터 25게임초 쿨, 미사용 방어 중첩 없음), 2단계 부적 5장 추가타(각 공격력 ×1), 3단계 모든 일반 스킬 쿨 -1초다. `wujiWardState`는 저장하지 않으며 집중 해제 시 준비를 버린다. 추적탄은 구체 한 개만 흡수하고 방패·패링·녹광 방호를 우선한다. 태허도검은 수묵 FX(`inkSchool`) 중 연계 피해 +20%, 생무극은 결정 방패·녹광 방호·패링 성공마다 공격력 ×5 부적 반격(먹문 흡수 제외)이다. 숙련은 `S.mastery.wuji`에 병합한다.
- 계열 카드 9장은 한 장씩 펼쳐 기존 `#book` 스킬 행을 재사용한다. 편성 4칸은 모바일 2×2, 선택/해제 후 기존 순서 배열에 맞춰 앞쪽부터 채운다. 칩(`#schoolEffects`)·전투 배지(`#battleEffects`)는 `schoolState()` 결과와 해당 효과 설명을 표시한다. 해금 배너는 UI의 `watchSchoolUnlocks`가 최고 스테이지 변화를 감지해 순서대로 표시하며 저장 불러오기에는 재생하지 않는다.

### 3-9. 배속·충전식 축복
- `S.speed`는 1/2(기본 1). `S.blessing`은 `charges`(처음 3회), `chargeAt`(충전 기준 시각), `until:{power,gold,haste}`(종료 시각), `pending:{id,readyAt}|null`(3초 수령 연출)을 저장한다. `fresh`/`load`에서 기본값·부분 객체를 병합하며 기존 저장 키/해금 바닥값은 유지한다.
- `syncBlessings`가 Date.now 기준 2시간마다 1회, 최대 3회 충전하고 종료 중 경과도 정산한다. 최대 충전 이후 초과 시간은 쌓지 않는다. 힘/풍요/신속은 각각 공격력 ×2/골드 ×2/배속 ×3, 30분씩 연장(최대 2시간), 동시 유지 가능하다. 선택 즉시 1회 소비·저장하고 3초 뒤 발동하므로 연출 중 재접속에도 중복 소비되지 않는다.
- 힘/풍요는 `stats`에서 반영하며 만료 즉시 재계산한다. 신속은 저장된 배속(`S.speed`)을 덮어쓰지 않아 종료 시 ×1/×2로 돌아간다. 신속 중에는 배속 버튼이 `S.hasteSpeed`를 ×3→×1→×2 순서로 바꾸며, 새 신속을 받을 때마다 ×3으로 시작한다. 오프라인 보상은 축복 없는 스탯과 기존 산식을 쓰며 배속을 곱하지 않는다. 모든 배율·충전/지속/연출 시간은 `BAL`에 둔다.

---

## 4. 연계 설계 규칙 (핵심)

- **모든 일반 스킬은 `COMBOS`의 정확히 한 쌍에 속한다.** 시작 스킬 `a` → 마무리 스킬 `b`.
- 시작 스킬을 쓰면 짝 스킬의 남은 쿨타임이 **`PRIME_CD`(3초) 이하**로 줄어든다(연계 준비).
- `comboWin()`(8초, 일일 도전 "끝없는 연계"면 16초) 안에 마무리 스킬을 쓰면 연계기가 발동한다.
- 발동 보상: 시작 스킬의 남은 쿨타임 **절반**, 각성 게이지 **+`BAL.comboGauge`(8)**.
- 자동 스킬 순서(`autoCast`): ① 열린 연계는 마무리(곧 준비되면 기다림) ② 두 스킬이 다 장착돼 있으면 시작 스킬부터 ③ 단독 사용. 단, 시작 스킬이 `HOLD_FOR`(6초) 안에 준비되는 마무리 스킬은 아껴 둔다.
- 결정 방패(`shield`)는 자동 스킬에서 보스 공격에 맞춰서만 쓴다.
- 스킬 바는 편성 순서대로 짝끼리 붙여 배치하고 사이에 ⛓를 표시한다(`barOrder`). 편성은 4쌍, 스킬 바는 최대 8칸이다.
- 측정 기준: 짝 3쌍 장착 120초 자동 전투에서 마무리 스킬 사용 15회 중 14회가 연계기였다. 이 비율이 떨어지는 변경은 되돌린다.

---

## 5. 새 콘텐츠 추가 체크리스트

> 스토리보드 시트로 스킬을 만들 때는 먼저 로컬 폴더의 `output/skill-storyboards-v2/GUIDE.md`를 읽는다(진행 순서, id·해금 배정, 시트 해석 규칙). 이 폴더는 저장소에 없고 커밋하지 않는다.

### 새 일반 스킬
1. `skills.js`에 `castXxx(pm)` 작성. 규칙: `pm`을 `skillHit`에 전달, `castLock` 유지, 몬스터가 사라져도 안전, 마지막 큰 한 방은 `heavy`+`name`, `sid`에 스킬 id.
2. `SK`에 `{id,name,unlock,cd,c,fn,d}` 추가(정렬은 `SK.sort`가 해 준다).
3. `IC[id]`에 32×32 SVG 아이콘.
4. `BR[id]`에 분기 a/b. 전용 효과가 없으면 `gen:1`(A 피해 +40%, B 쿨타임 -30%가 자동 적용).
5. **반드시 짝을 만든다:** 기존 스킬과 짝이 안 맞으면 스킬 두 개를 함께 추가해 새 연계기를 만든다.
6. `?selftest` 통과 확인.

### 새 연계기
`comboXxx(pm=1)` 작성(`pm`을 `skillHit`에 전달, 마지막은 `heavy`+`crack`+연계기 이름) → `COMBOS`에 `{a,b,name,fn,col,d}`. 컷인·패널 카드·버튼 배지·안내 칩은 자동으로 생긴다.
- 시작 연출을 인계하려면 `keep:{get,ready,release}`를 추가한다: 시작 함수는 FX를 반환하고, `get()`은 살아 있는 FX, `ready`는 시작 연출 종료 시각(초), `release(fx)`는 `collapse` 표시와 종료 처리이며, 공통 경로가 유지·전체 시연·중복 마무리 방지·`fn(pm,fx)` 인계(`claimed`)를 담당한다. 연계 함수는 FX를 소모(`consumed`)하고 종료 시간을 정한다.
- 연계의 `school`과 `SCHOOLS`를 맞춘다. 모든 연계는 한 계열에만 속하고, 계열마다 정확히 2쌍이다.

### 새 각성기
`awkXxx(pm=1)` 작성(첫 줄에 `cutin(...)`, `castLock` 설정) → `AWK`에 `{id,name,unlock,c,fn,d}` → 아이콘은 `AWK_IC[id]` 또는 `IC[id]`. 각성기 id는 일반 스킬 id와 겹치면 안 된다.

### 그 밖
- 보스 패턴: `startPat`의 목록, `bossAI` 분기, `drawBossPat`/`drawRain` 경고 표시, 적중 시각 `imp`. 경고 없이 맞히는 패턴 금지(패링 기회를 줘야 한다).
- 유물 `RELICS`, 업적 `ACH`(`p()`는 `[현재, 목표]`), 일일 도전 조건 `DMODS`+`chMul`.

---

## 6. 확인·테스트 방법

1. **정적 서버로 띄운다.** 파일을 직접 열면(file://) 안 되는 경우가 있다. 예: `python -m http.server 5173` 또는 `npx serve -l 5173`.
2. **`/?selftest`로 연다.** 저장을 끈 채 새 상태에서 모든 스킬(분기 3종씩)·연계기·각성기·보스 등장·2페이즈·패턴·재도전 쿨타임 초기화·일일 도전을 실제로 돌리고 오른쪽 위에 결과를 띄운다. 결과는 `window.__selftest`에도 남는다. **실패가 0이 아니면 푸시하지 않는다.**
3. **눈으로 확인한다.** 연출 변경은 실제 화면에서 봐야 한다. 패널의 "시연" 버튼으로 잠긴 스킬·연계기·각성기도 바로 볼 수 있다.
4. 주의: 브라우저 탭이 숨겨지면 `requestAnimationFrame`이 멈춰 게임이 정지한다. 자동화 검사는 `update(1/60);render()`를 직접 반복 호출해 프레임을 진행시킨다(`selftest.js`의 `tick`). 속도를 위해 `render()`는 4프레임마다 하고, 시연은 `settle`로 연출이 끝나면 바로 다음으로 넘어간다. 전체 점검은 약 30~40초이며, 긴 자동 전투 검사는 `update`만 돌린다. 점검이 1분을 넘기게 만드는 변경은 피한다.
5. 파일을 도구로 치환할 때 `\b` 같은 이스케이프가 제어 문자로 바뀌지 않았는지 확인한다(실제로 백스페이스 문자가 들어가 점검 파일이 안 불러와진 적이 있다).
6. ⚠️ **기존 검사를 지우거나 느슨하게 만들지 않는다.** 검사가 실패하면 코드를 고친다. 검사 자체가 틀렸다고 판단되면 고치지 말고 이유를 PR에 적어 사용자 확인을 받는다. 새 기능에는 검사를 추가한다. 보고할 때는 통과·실패 수를 함께 적는다(검사 수가 줄었다면 설명이 필요하다).

---

## 7. 배포와 커밋

- `main`에 푸시하면 GitHub Pages가 1~2분 안에 자동 배포한다. 빌드 없음.
- 커밋 메시지는 영어로, 첫 줄에 무엇을 했는지, 본문에 이유와 수치를 적는다. 기능 단위로 커밋한다.
- 푸시 전: `?selftest` 통과 + 연출 변경은 화면 확인.
- **AI 도구는 `main`에 직접 푸시하지 않는다.** 새 브랜치에서 작업하고 Pull Request로 올린다. 사용자가 검토한 뒤 합친다.
- ⚠️ **저장소에는 게임 실행에 필요한 파일과 문서만 커밋한다.** 스크린샷·녹화·로그·임시 파일은 커밋하지 말고 PR 설명에 첨부한다. `.gitignore`가 이미지·영상·로그를 막고 있으니 우회(`git add -f`)하지 않는다.

---

## 8. 현재 콘텐츠 지도

### 일반 스킬 36개 = 연계기 18쌍
| 시작 스킬 (해금) | 마무리 스킬 (해금) | 연계기 |
|---|---|---|
| 원소 질주 `dash` (1) | 네온 연무 `neon` (45) | 프리즘 질주 |
| 심연 비검 `swords` (5) | 차원문 `portal` (96) | 차원 검우 |
| 마룡 숨결 `breath` (10) | 진홍 대포 `cannon` (26) | 진홍 섬멸 |
| 그림자 분신 `shadow` (15) | 일식 `eclipse` (38) | 월식 처형 |
| 화염 채찍 `whip` (20) | 악마 참격파 `dslash` (32) | 업화 난무 |
| 그림자 손아귀 `hands` (52) | 망령 해골 `skulls` (68) | 망자의 연회 |
| 결정 방패 `shield` (60) | 녹광 저격 `sniper` (114) | 결정 굴절포 |
| 악귀 변신 `demon` (76, 버프) | 진홍 승천격 `upper` (120) | 혈귀 염마 |
| 녹광 구체 `orb` (84) | 괴수 돌진 `beast` (108) | 녹광 괴수탄 |
| 망령 궁수대 `archers` (90) | 그림자 늑대 `wolf` (102) | 사냥의 밤 |
| 뇌창 투척 `spear` (128) | 천뢰 낙하 `thunder` (136) | 뇌신 강림 |
| 중력 구속 `gravity` (144) | 운석 낙하 `meteor` (152) | 천붕 |
| 서리 월참 `frostcut` (160) | 백룡 돌진 `icedragon` (170) | 빙룡쇄파 |
| 빙화 장벽 `iceflower` (180) | 동결 나선 `frostspiral` (190) | 만화빙정 |
| 쌍획참 `twinstroke` (200) | 백영보 `whitestep` (210) | 양의교차 |
| 묵우검 `inkrain` (220) | 반월경 `halfmoon` (230) | 묵경반천 |
| 축지묵문 `ink_gate` (240) | 양의속박 `bind` (250) | 묵문봉인 |
| 백지귀환 `talisman` (260) | 역류묵하 `backflow` (270) | 백지환류 |

- 뇌신 강림과 천붕은 `keep` 훅으로 시작 스킬의 창/구체를 8초/도전 16초 연계 창 동안 보존해 마무리 스킬에 인계하며, 만료·중단 시 전기를 잃고 부서지거나 붕괴한다. 시연은 시작 스킬부터 재생한다.
- 피해/쿨타임: 중력 구속 `(9+1.2×TR)배/20초`, 운석 낙하 `(13.5+1.9×TR)배/26초`, 천붕 추가 16배. 기본 초당 배율 0.450/0.519로 뇌창·천뢰의 0.456/0.542 이하이며, 일반 스킬 분기는 A 피해 +40% / B 쿨타임 -30%다.
- 빙룡쇄파/만화빙정은 기존 `keep` 훅으로 가시 3개/꽃잎 6개의 원본 객체를 인계해 순차 파괴/흡수한다. 빙화 장벽은 공격용이며 방패 차단은 없다. 빙정 전용 연출은 `#05070B/#245BBC/#697582/#61DCF3/#FFFFFF` 단색 5층, 흰 중심 가산 합성이며, 시작부터 연계 종료까지 입자는 각각 90/100개다.
- 빙정 피해/쿨타임: 서리 월참 `(9.5+1.2×TR)/22초`, 백룡 돌진 `(14+1.9×TR)/28초`, 빙화 장벽 `(10+1.2×TR)/24초`, 동결 나선 `(14.5+1.9×TR)/30초`. 기본 초당 배율 0.432/0.500/0.417/0.483으로 중력/운석의 0.450/0.519 이하(TR 0~3도 동일)이며, 연계 추가 피해는 천붕과 같은 16배, 분기는 A +40% / B 쿨타임 -30%다.
- 음양검결은 흑백 초승달 획 2개/박힌 먹검 5개의 원본 배열과 객체를 `keep`으로 인계해 잔상 통과·X자 폭발/회수·반사를 이어 간다. 수묵 FX는 매 월드 프레임 `desat=1`, 전용 연출은 `#05070B/#363F4D/#77808C/#D9DFE5/#FFFFFF` 5색이며 금색은 양의개천에만 쓴다.
- 수묵 피해/쿨타임(`BAL.ink`): 쌍획참 `(9+1.2×TR)/24초`, 백영보 `(13.5+1.8×TR)/30초`, 묵우검 `(9.5+1.2×TR)/26초`, 반월경 `(14+1.8×TR)/32초`. 기본 초당 배율 0.375/0.450/0.365/0.438로 대응 빙정 스킬 이하(TR 0~3, 동일 분기도 동일). 연계 추가 16배, 분기 A +40% / B 쿨타임 -30%다.
- 무극도법은 먹문 2개에서 띠가 뻗어 봉인하고, 부적 5장을 파도가 순차 흡수한다. 모두 원본 `keep` 객체를 인계하며 공용 시전 함수는 그대로다. 수묵 5색·매 프레임 `desat=1`, 금색은 무극귀일에만 사용한다.
- 무극 피해/쿨타임(`BAL.wuji`): 축지묵문 `(9+1.2×TR)/26초`, 양의속박 `(13.5+1.8×TR)/32초`, 백지귀환 `(9.5+1.2×TR)/28초`, 역류묵하 `(14+1.8×TR)/34초`. 음양검결과 같은 총 배율에 쿨타임 +2초로 기본 초당 배율 0.346/0.422/0.339/0.412이며 TR 0~3·동일 분기에서도 이하. 연계 추가 16배, 분기 A +40% / B 쿨타임 -30%다.

### 각성기 (게이지 100%, 하나 선택)
천검멸 `thousand`(1) · 암흑 마법진 `circle`(40, 12초간 쿨타임 2배 속도) · 그림자 용 `dragon`(81) · 염마 강림 `inferno`(111) · 영원의 설관 `frostcrown`(130). 그림자 용·염마 강림은 원래 일반 스킬이던 것을 옮겼고 위력 ×2.5. 설관은 얼음 검·여섯 결정·휘감는 백룡 후 빙정이 폭발하며 `(48+6×TR)배/게이지 100`; 염마 강림의 60fps 측정 `(50.5+6.25×TR)배` 이하로 정했다.
- 녹광 전용 천체 창례 `celestial`(140, 천체 파쇄→녹광 궤도 압축→검은 핵 폭발) · 뇌전 전용 천벌 병기 `judgment`(160, 집속 고리 3개→거대 번개 창 낙하) · 업화 전용 업화 명왕 `hellking`(185, 큰 실루엣과 병기 3개→주황 업화 강타). 각 2.8초·게이지 100, 피해는 `(44+5×TR)/(46+5.5×TR)/(48+6×TR)배`로 전 단계에서 설관 이하이며, 집중 시 각각 ×1.5다. 전용 도형은 검정·어두운 계열색·회색·밝은 계열색·흰 중심 5층, 입자 74개이며 천체/천벌의 준비 구간은 매 프레임 `desat`을 유지한다. 같은 스테이지의 계열·각성기 해금 배너는 순서대로 표시한다.

- 음양검결 전용 양의개천 `yinyangsky`(205): 흑백 구슬·쌍기둥→태극과 거대 검→금빛 경계 파쇄. 2.8초·게이지 100, `(48+6×TR)배`로 설관과 같으며 집중 시 ×1.5다.
- 무극도법 전용 무극귀일 `wuji_return`(255): 먹 원환→파편과 참격의 수렴→백색 공간 폭발→한 점으로 소멸. 2.8초·게이지 100, `(48+6×TR)배`로 양의개천과 같으며 집중 시 ×1.5다.
- 무계열 수묵 각성기 태극묵륜 `taichi`(235): 검 기 모으기→흑백 초승달 획 분리→태극 형성→검격 절단→사분할 비산→소멸. 2.8초·게이지 100, `(48+6×TR)배`로 양의개천 이하이며 계열이 없어 집중 보너스는 없다.

### 기타 시스템
강화 9종(`UP`) · 스킬 각성 분기(해금 스테이지+8, 골드) · 연출 단계(비전 숙련 Lv 5/10/20) · 동료(궁수·마법사) · 유물 12종·4등급 · 업적 16개와 칭호 · 환생(STAGE 30 이상, 유물·업적·각성은 유지) · 일일 도전 · 보스 2페이즈 · 오프라인 보상(최대 8시간) · 보스 도감(13종, 칸당 피해 +2%, 최대 +26%).

### 밸런스 공식
몬스터 체력 `12×1.3^(stage-1)`, 보스 ×7(도전 보스 ×1.5 추가) · 골드 `4×1.19^(stage-1)` · 스테이지당 8마리 · 스킬 각성 비용 `goldDrop(해금+12)×40`.
- 수치는 `core.js`의 `BAL`(체력·골드 증가율, 연계기 게이지 +8, 스킬 적중 게이지 0.8, 배속·축복 수치) 한곳에 둔다. 2배속·축복 사용 기준 측정(v3): 첫 집중 약 8분, 첫 환생 약 30분, 빙정 완성 약 4시간, 음양검결 시작 약 4.5시간·완성 8시간 이후. 각성기 피해 비중 약 28%(3시간 기준).
- **해금 판정은 반드시 `ub()`(해금 기준 스테이지)나 `unlocked()`로 한다.** `S.best`와 직접 비교하지 않는다. 예전 저장은 `OLD_UNLOCK`→`NEW_UNLOCK`으로 환산한 `S.unlockFloor`를 받아 기존 28스킬을 보존한다. 바닥값 상한은 `NEW_UNLOCK` 마지막 값(190)이며, 저장된 옛 9999도 `load()`에서 보정한다. `ub()`는 실제 최고 기록과 바닥값 중 큰 값이므로 옛 최고 72는 수묵 잠금, 최고 250은 수묵 해금이다. 신규 스킬은 역사적 변환표에 덧붙이지 않는다.

---

## 9. 다음에 해 볼 만한 것 (사용자와 합의 전)
- 스킬별 전용 효과음(지금은 여러 스킬이 소리를 나눠 씀)
- 사용자가 거절한 것: 세이브 코드 내보내기·불러오기(필요 없다고 함)

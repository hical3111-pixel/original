# 스크린샷·녹화·로그·임시 파일은 커밋하지 않는다 (AGENTS.md 7장)
*.png
*.jpg
*.jpeg
*.gif
*.webp
*.mp4
*.webm
*.log
*.tmp

# 로컬 전용 작업물(스킬 시트·스토리보드). 저장소에 올리지 않는다
output/

# AI 도구가 만드는 작업 폴더(git worktree). 저장소에 올리지 않는다
.*-worktree/
